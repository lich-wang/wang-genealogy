# Luna 王姓扩展第二阶段审核

## 阶段结果

以只读快照 `/tmp/wang-expansion-fM1AHK/wang.sqlite` 为身份基线。快照有 2,661 条人物记录，其中 active 1,996、suppressed 665；有 8,567 条 Claim，其中关系 Claim 2,350。

本阶段从 Wikidata 王姓（P734）人物的后续排序区间获取 811 位候选。经卒年、姓名、QID、跨批次同名及日期逻辑审核后，通过 675 位，排除 136 位。另复用上一阶段 Luna 已通过的 273 条关系（亲子 210、配偶 61、收养 2）及其所需关系人物。合并计划共有 772 位待解析人物、273 条关系、1210 个来源。

## 来源与历史性

新增 roster 人物必须同时满足：

- Wikidata 声明为 human（P31=Q5）；
- family name 明确为王（P734=Q804943）；
- 中文标签以“王”开头；
- 有明确卒年 P570，且早于 1950 年；
- 每人保存稳定 `wd:QID` key、QID 外部标识、CC0 来源 URL 和 `historicity.kind=death_date`。

关系沿用上一轮逐边审核结果，证据来自双方 Wikidata 陈述或 CBDB；“嗣父”单列为 `adoptive_parent`。未把 rejected/manual_review 边带入。

## 排除统计

- 本批次出现折叠同名但不同 QID，全部留待人工辨析：115
- 与待导入亲属人物同名，避免自动复用或创建歧义记录：2
- 结构化寿命超过 120 年，需人工核对：18
- 缺少符合门槛的 1950 年前卒年：1

完整排除对象记录在计划的 `skipped`，不会进入导入人物数组。

## 同名与隐私控制

- 与快照的 QID 和折叠姓名去重由抓取器完成；合并四批后再次检查批次间折叠同名。
- 与亲属计划中新人物同名者也从 roster 排除，不自动复用。
- 没有明确本名、含拉丁占位符或仅“王某”的对象排除。
- 明确卒于 1950 年以前，因此不收录在世人物；计划不含联系方式、住址、账号等敏感字段。

## 导入约束

文件 `scripts/luna-wang-expansion-stage2-2026-09.json` 是可复核建议，不直接写 D1。导入前仍须使用最新快照执行 `import-kinship.mjs --dry-run`。最小写入规则要求导入器对已存在 QID、来源、Claim 和关系执行零写入 no-op。
